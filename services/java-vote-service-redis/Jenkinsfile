node {
    def app

    stage('Clone repository') {
        checkout scm
    }

    stage('Package jar file with Maven') {
        withMaven(
            // Maven installation declared in the Jenkins "Global Tool Configuration"
            maven: 'M3') {
            // Run the maven build
            sh "mvn -f services/java-vote-service-redis/pom.xml clean install"
        } 
    }

    stage('Build image with Docker') {
        app = docker.build(
            "services/java-vote-service-redis:${env.BUILD_NUMBER}",
            "services/java-vote-service-redis/Dockerfile"
            )
    }

   stage('Push image with to container registry') {

        docker.withRegistry('https://sabbour.azurecr.io', 'acr-hub-credentials') {
            app.push("services/java-vote-service-redis:${env.BUILD_NUMBER}")
            app.push("services/java-vote-service-redis:latest")
        }
    }
}