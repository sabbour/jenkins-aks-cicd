kind: Service
apiVersion: v1
metadata:
  name: python-vote-service
  namespace: XX_NAMESPACE_XX
spec:
  selector:
    app: python-vote-service
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
